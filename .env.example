# FLUX API Configuration

# Network Settings
HOST=0.0.0.0
PORT=13005
DEBUG_MODE=False

# Security Settings
REQUIRE_API_KEY=False
API_KEY=

# Model Settings
MODEL_ID=sayakpaul/FLUX.1-merged
QUANTIZATION=nf4
DEVICE=cuda
TORCH_DTYPE=bfloat16

# Default Inference Parameters
DEFAULT_STEPS=4
DEFAULT_GUIDANCE_SCALE=0.0
DEFAULT_WIDTH=1024
DEFAULT_HEIGHT=1024
DEFAULT_STRENGTH=0.60
PREWARM=True

# Hugging Face Access Token (optional, only needed for gated models like FLUX.1-dev)
HF_TOKEN=
